name: Deploy to Production

on:
  push:
    tags:
      - 'v1*'  # escucha tags que empiecen con v1
  release:
    types: [published]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy app
        run: |
          echo "🚀 Desplegando la versión ${{ github.ref }}"